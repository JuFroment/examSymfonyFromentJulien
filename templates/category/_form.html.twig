<div class="container">
{{ form_start(form) }}
    <div class="row">
        <div class="offset-3 col-2">
            {{ form_label(form.libelle, 'Nom de la catégorie', {'label_attr':{'class':'mt-3'}}) }}
        </div>
        <div class="col-4">
            {{ form_widget(form.libelle, {'attr': {'class': 'form-control mt-3'}} ) }}
        </div>
    </div>
    <div class="text-center my-3">
    <button class="btn btn-success">{{ button_label|default('Créer') }}</button>
    </div>
{{ form_end(form) }}
</div>